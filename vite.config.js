import { defineConfig } from 'vite';

export default defineConfig({
  // Relative base so the build works at any path — e.g. GitHub Pages at
  // /html-in-canvas-cracks/ and also `vite preview` at /.
  base: './',
});
